{% extends 'base.html.twig' %}

{% block title %}Add trick
{% endblock %}

{% block body %}

	<div class="container">

		{{ form_start(form) }}

		{{ form_widget(form) }}

		{{ form_row(form.groupsId) }}

		<div class="text-center">
			<button type="submit" class="btn btn-primary round mt-3">Save</button>
		</div>
		{{ form_end(form) }}
	</div>
{% endblock %}
